service:
  type: NodePort
  nodePort: 32000

adminPassword: "k8s-soc-admin"

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        url: http://loki:3100
        isDefault: true
        jsonData:
          maxLines: 1000
